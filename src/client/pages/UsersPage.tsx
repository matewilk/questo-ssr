import React from "react";

const center = {
  display: "grid",
  justifyItems: "center",
};

const UsersPage = () => (
  <div style={center}>
    <div>I'm the users page</div>
  </div>
);

export default {
  component: UsersPage,
};
